<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Request Refund | Supermarket</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f5f7fa;
        }

        .refund-container {
            max-width: 700px;
            margin: auto;
            margin-top: 40px;
        }

        .card-section {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.06);
        }

        .section-title {
            font-weight: bold;
            color: #c53030;
            margin-bottom: 10px;
        }

        .muted-text {
            color: #6c757d;
        }
    </style>
</head>

<body>
    <% if (success && success.length > 0) { %>
        <div class="alert alert-success text-center m-0">
            <%= success %>
        </div>
    <% } %>

    <% if (error && error.length > 0) { %>
        <div class="alert alert-danger text-center m-0">
            <%= error %>
        </div>
    <% } %>

    <div class="refund-container">
        <div class="card-section">
            <h3 class="section-title">Request Refund</h3>
            <p class="muted-text">
                Submit a refund request for this transaction. Our admin team will review it.
            </p>

            <hr>

            <form method="POST"
                  action="/api/refunds/request"
                  enctype="multipart/form-data">

                <!-- Transaction ID -->
                <input type="hidden" name="transactionId" value="<%= transactionId %>">

                <div class="mb-3">
                    <label class="form-label fw-bold">Transaction ID</label>
                    <input type="text"
                           class="form-control"
                           value="<%= transactionId %>"
                           disabled>
                </div>

                <!-- Refund Reason -->
                <div class="mb-3">
                    <label class="form-label fw-bold">Reason for Refund</label>
                    <textarea
                        class="form-control"
                        name="reason"
                        rows="4"
                        placeholder="Explain why you are requesting a refund"
                        required></textarea>
                </div>

                <!-- Optional Image Upload -->
                <div class="mb-3">
                    <label class="form-label fw-bold">
                        Supporting Image (optional)
                    </label>
                    <input type="file"
                           class="form-control"
                           name="image"
                           accept="image/*">
                    <small class="text-muted">
                        Upload a screenshot or photo if applicable.
                    </small>
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <a href="/viewProfile" class="btn btn-outline-secondary">
                        Cancel
                    </a>

                    <button type="submit" class="btn btn-danger px-4">
                        Submit Refund Request
                    </button>
                </div>
            </form>
        </div>
    </div>

</body>
</html>
