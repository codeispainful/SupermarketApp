<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User | Supermarket App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <%- include('partials/adminnavbar') %>

        <div class="container">
            <br>
            <div class="text-center">
                <h2>Edit User</h2>
            </div>
            <br>

            <div class="text-center">
                <% if (user) { %>
                    <form action="/editUser/<%= user.userid %>" method="POST">

                        <label for="username">Username:</label><br>
                        <input type="text" id="username" name="user" value="<%= user.username %>" required><br><br>

                        <label for="email">Email:</label><br>
                        <input type="email" id="email" name="email" value="<%= user.email %>" required><br><br>

                        <label for="contact">Contact:</label><br>
                        <input type="text" id="contact" name="contact" value="<%= user.contact %>"><br><br>

                        <label for="password">Password:</label><br>
                        <input type="password" id="password" name="password" value="<%= user.password %>"><br><br>

                        <label for="role">Role:</label><br>
                        <select id="role" name="role" required>
                            <option value="user" <%=user.role==='user' ? 'selected' : '' %>>User</option>
                            <option value="admin" <%=user.role==='admin' ? 'selected' : '' %>>Admin</option>
                        </select><br><br>

                        <label for="banned">Banned:</label><br>
                        <!-- Hidden input ensures 0 is sent if unchecked -->
                        <input type="hidden" name="banned" value="0">
                        <input type="checkbox" id="banned" name="banned" value="1" <%=user.banned==1 ? 'checked' : ''
                            %>><br><br>

                        <button type="submit" class="btn btn-primary">Update User</button>
                    </form>
                    <% } else { %>
                        <p>No user found.</p>
                        <% } %>
                            <a href="/adminViewUsers" class="btn btn-secondary mt-3">Back</a>
            </div>
        </div>
</body>

</html>