<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - Supermarket System</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        body {
            background: linear-gradient(to bottom right, #e8f5e9, #f3faf3);
            font-family: "Segoe UI", sans-serif;
        }

        .login-card {
            max-width: 420px;
            margin: 80px auto;
            padding: 35px;
            background: #ffffff;
            border-radius: 18px;
            border: 1px solid #dce6dd;
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.08);
        }

        .brand-title {
            font-weight: 800;
            color: #2e7d32;
            letter-spacing: 0.5px;
        }

        .brand-icon {
            font-size: 2.4rem;
        }

        .form-label {
            font-weight: 600;
            color: #2e2e2e;
        }

        .form-control {
            border-radius: 10px;
            padding: 10px 12px;
        }

        .btn-supermarket {
            background: #43a047;
            border: none;
            padding: 10px;
            font-size: 1.05rem;
            border-radius: 10px;
        }

        .btn-supermarket:hover {
            background: #388e3c;
        }

        a {
            color: #2e7d32;
            font-weight: 600;
        }

        a:hover {
            color: #1b5e20;
        }
    </style>
</head>

<body>

    <div class="login-card">

        <div class="text-center mb-3">
            <div class="brand-icon">🛒</div>
            <h2 class="brand-title mt-1">Supermarket Login</h2>
        </div>

        <% if (error && error.length > 0) { %>
            <div class="alert alert-danger text-center">
                <%= error[0] %>
            </div>
        <% } %>

        <form action="/loginUser" method="POST">
            <div class="mb-3">
                <label for="username" class="form-label">Username</label>
                <input 
                    type="text"
                    id="username"
                    name="username"
                    class="form-control"
                    placeholder="Enter your username"
                    required
                />
            </div>

            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input 
                    type="password"
                    id="password"
                    name="password"
                    class="form-control"
                    placeholder="Enter your password"
                    required
                />
            </div>

            <button type="submit" class="btn btn-supermarket text-white w-100 mt-2">
                Login
            </button>
        </form>

        <p class="text-center mt-3">
            Don’t have an account?  
            <a href="/registerUser" class="text-decoration-none">Register here</a>
        </p>
    </div>

</body>
</html>