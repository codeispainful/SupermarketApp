<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - Supermarket App</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom right, #e8f5e9, #f3faf3);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: "Segoe UI", sans-serif;
        }

        .register-card {
            background: white;
            padding: 35px;
            border-radius: 18px;
            max-width: 460px;
            width: 100%;
            border: 1px solid #d6e6d8;
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.08);
        }

        .register-title {
            font-weight: 800;
            text-align: center;
            color: #2e7d32;
            letter-spacing: 0.5px;
        }

        .brand-icon {
            font-size: 2.3rem;
        }

        .form-label {
            font-weight: 600;
            color: #2e2e2e;
        }

        .form-control {
            padding: 10px 12px;
            border-radius: 10px;
        }

        .btn-register {
            background-color: #43a047;
            color: white;
            border-radius: 10px;
            padding: 10px;
            font-size: 1.05rem;
            border: none;
        }

        .btn-register:hover {
            background-color: #388e3c;
        }

        a {
            color: #2e7d32;
            font-weight: 600;
            text-decoration: none;
        }

        a:hover {
            color: #1b5e20;
            text-decoration: underline;
        }

        .alert {
            border-radius: 10px;
        }
    </style>
</head>

<body>
    <div class="register-card">
        <div class="text-center mb-3">
            <div class="brand-icon">🛒</div>
            <h2 class="register-title">Create Your Account</h2>
        </div>

        <% if (error && error.length>0) { %>
            <div class="alert alert-danger text-center m-0"><%= error %></div>
        <% } %>

        <form action="/registerUser" method="POST">

            <div class="mb-3">
                <label class="form-label" for="username">Username</label>
                <input type="text" class="form-control" id="username" maxlength="20" minlength="8"
                    name="username" placeholder="Choose a username" required>
            </div>

            <div class="mb-3">
                <label class="form-label" for="password">Password</label>
                <input type="password" class="form-control" id="password" maxlength="30" minlength="8"
                    name="password" placeholder="Create a password" required>
            </div>

            <div class="mb-3">
                <label class="form-label" for="email">Email Address</label>
                <input type="email" class="form-control" id="email"
                    name="email" placeholder="Enter your email" required>
            </div>

            <div class="mb-3">
                <label class="form-label" for="contact">Contact Number</label>
                <input type="text" class="form-control" id="contact" maxlength="8" minlength="8"
                    name="contact" placeholder="Enter your phone number" required>
            </div>

            <button type="submit" class="btn btn-register w-100 mt-2">
                Register
            </button>
        </form>

        <p class="text-center mt-3">
            Already have an account?  
            <a href="/loginUser">Login here</a>
        </p>
    </div>
</body>
</html>